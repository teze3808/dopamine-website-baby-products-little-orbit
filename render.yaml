services:
  - type: web
    runtime: static
    name: dopamine-website-baby-products-little-orbit
    buildCommand: pnpm install --frozen-lockfile && pnpm build
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
